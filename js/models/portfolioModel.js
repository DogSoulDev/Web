// portfolioModel.js// portfolioModel.js// portfolioModel.js// portfolioModel.js

// Data model for portfolio projects

// Data model for portfolio projects

const portfolioModel = {

  projects: [],// Data model for portfolio projects// Data model for portfolio projects

  addProject: function(project) {

    // Validate and add projectconst portfolioModel = {

    this.projects.push(project);

  }  projects: [],

};

  addProject: function(project) {

    // Validate and add projectconst portfolioModel = {const portfolioModel = {

    this.projects.push(project);

  }  projects: [],  projects: [],

};

  addProject: function(project) {  addProject: function(project) {

    // Validate and add project    // Validate and add project

    this.projects.push(project);    this.projects.push(project);

  }  }

};};


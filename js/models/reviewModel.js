// reviewModel.js// reviewModel.js// reviewModel.js// reviewModel.js

// Data model for user reviews

// Data model for user reviews

const reviewModel = {

  reviews: [],// Data model for user reviews// Data model for user reviews

  addReview: function(review) {

    // Validate and add reviewconst reviewModel = {

    this.reviews.push(review);

  }  reviews: [],

};

  addReview: function(review) {

    // Validate and add reviewconst reviewModel = {const reviewModel = {

    this.reviews.push(review);

  }  reviews: [],  reviews: [],

};

  addReview: function(review) {  addReview: function(review) {

    // Validate and add review    // Validate and add review

    this.reviews.push(review);    this.reviews.push(review);

  }  }

};};

